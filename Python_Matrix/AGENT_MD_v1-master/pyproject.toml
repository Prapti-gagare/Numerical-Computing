[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "matrixpy"
version = "0.0.0"
description = "Python port of Matrix C++ utilities"
dependencies = ["numpy", "scipy"]
